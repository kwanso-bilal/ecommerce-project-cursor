PORT=3000
NODE_ENV=development

# Database (required for app and seed)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
DATABASE_NAME=ecommerce-project-cursor

# JWT
JWT_SECRET=change-me-in-production

# Verification email link (optional)
FRONTEND_VERIFY_URL=http://localhost:5173

# SMTP (optional; if not set, verification link is logged to console)
# MAIL_HOST=
# MAIL_PORT=587
# MAIL_USER=
# MAIL_PASS=
# MAIL_FROM=
